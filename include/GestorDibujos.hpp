#pragma once
#include "Dibujo.hpp"

class GestorDibujos {
public:
    static Dibujo obtenerPajaro();
    static Dibujo obtenerPajaroSorprendido();
    static Dibujo obtenerManzana();
    static Dibujo obtenerPistola();
    static Dibujo obtenerAngel();
    static Dibujo obtenerSol();
    static Dibujo obtenerNube();
    static Dibujo obtenerCazador();
    static Dibujo obtenerArbol();
};
